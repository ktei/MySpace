<navigation:Page x:Class="LiteApp.MySpace.Views.PhotosView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cal="http://www.caliburnproject.org"
    xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
    xmlns:converters="clr-namespace:LiteApp.MySpace.Converters"
    xmlns:controls="clr-namespace:LiteApp.MySpace.Controls"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    mc:Ignorable="d" Style="{StaticResource PageStyle}"
    d:DesignHeight="300" d:DesignWidth="400">
    <navigation:Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="boolToVis"/>
        <converters:BooleanToVisibilityConverter x:Key="boolToVisReverse" Reverse="True"/>
    </navigation:Page.Resources>

    <Grid>
        <ScrollViewer x:Name="PageScrollViewer" Style="{StaticResource PageScrollViewerStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="Photos" />
                <StackPanel Orientation="Horizontal">
                    <!--<Button Height="30" Content="Upload Photo" Width="150" HorizontalAlignment="Left" cal:Message.Attach="UploadPhoto"/>-->
                    <Button Height="30" Content="Create Album" Width="150" HorizontalAlignment="Left" cal:Message.Attach="CreateAlbum" Style="{StaticResource BlueButton}"/>
                </StackPanel>

                <StackPanel>
                    <ItemsControl ItemsSource="{Binding UploadItems, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,10,0,0" HorizontalAlignment="Left" Width="400">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource ContentTextStyle}" Text="{Binding FileName, Mode=OneWay}" TextTrimming="WordEllipsis"/>
                                    <HyperlinkButton Grid.Column="1" Content="Cancel" Visibility="{Binding CanCancel, Converter={StaticResource boolToVis}, Mode=OneWay}" cal:Message.Attach="CancelUpload"/>
                                    <TextBlock Style="{StaticResource ContentTextStyle}" Text="{Binding Status, Mode=OneWay}" Grid.Column="1" 
                                               Visibility="{Binding CanCancel, Converter={StaticResource boolToVisReverse}, Mode=OneWay}"/>
                                    <ProgressBar Value="{Binding Progress, Mode=OneWay}" Height="10" Maximum="1" Minimum="0" Grid.Row="1" Grid.ColumnSpan="2"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <ItemsControl x:Name="Albums" ItemsSource="{Binding Albums, Mode=OneWay}" Visibility="{Binding IsBusy, Mode=OneWay, Converter={StaticResource boolToVisReverse}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" Margin="0,15,0,0" Padding="0,5" BorderBrush="Black" Width="870">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid MaxWidth="256" Height="158">
                                        <Border Style="{StaticResource PictureStyle}" Visibility="{Binding IsLoadingCover, Mode=OneWay, Converter={StaticResource boolToVisReverse}}">                                            
                                            <Image Source="{Binding CoverUri, Mode=OneWay}" ImageOpened="Image_ImageOpened" />
                                        </Border>
                                        <controls:LoadingIndicator Style="{StaticResource WhiteIndicator}"
                                                               Visibility="{Binding IsLoadingCover, Mode=OneWay, Converter={StaticResource boolToVis}}"/>
                                    </Grid>
                                    
                                    <Grid Grid.Column="1" Margin="10,0,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{Binding Name, Mode=OneWay}"
                                            TextWrapping="NoWrap" Style="{StaticResource ContentTextStyle}" FontSize="25" FontWeight="ExtraLight"  TextTrimming="WordEllipsis" Width="600"/>
                                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Grid.Row="1" BorderThickness="0" Height="80">
                                            <TextBlock Text="{Binding Description, Mode=OneWay}" Style="{StaticResource ContentTextStyle}" 
                                                       FontStyle="Italic" TextWrapping="Wrap" />
                                        </ScrollViewer>
                                        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
                                            <Button Height="30" Content="Upload Photo" Width="120" Margin="0,0,10,0" Style="{StaticResource BlueButton}" />
                                            <Button Height="30" Content="Delete Album" Width="120" CommandParameter="{Binding Id, Mode=OneWay}"
                                                    Style="{StaticResource RedButton}" Command="{Binding DataContext.DeleteAlbumCommand, Mode=OneWay, ElementName=Albums}"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <controls:LoadingIndicator Style="{StaticResource WhiteIndicator}" Margin="0,100,0,0"
                                           Visibility="{Binding IsBusy, Mode=OneWay, Converter={StaticResource boolToVis}}" />
                <sdk:DataPager Style="{StaticResource DefaultPager}" Source="{Binding Albums}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</navigation:Page>
